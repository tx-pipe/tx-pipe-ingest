# ---- Confluent Schema Registry Settings (using in docker build) ----
CONFLUENT_SR_URL=
CONFLUENT_SR_API_KEY=
CONFLUENT_SR_API_SECRET=
CONFLUENT_PROTO_SUBJECTS= # kafka topics with "-value" postfix by default | comma-separated
